import React from 'react'

import styles from './Spinner.css'

/**
 * Spinner is used to showcase loading state
 */
function Spinner() {
  return (
    <div className={styles.spinner}>
      <div />
      <div />
    </div>
  )
}

export default Spinner
